@model IEnumerable<Ads.Models.Tuser>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Username)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Pass)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.IdRolNavigation.DescripcionRol)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Username)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Pass)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.IdRolNavigation.DescripcionRol)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdUsers">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IdUsers">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IdUsers">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
